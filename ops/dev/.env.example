COMPOSE_PROJECT_NAME=dota2-coach

# Set DISCORD_CREDENTIALS_PATH only when Discord is enabled.
DISCORD_CREATE_PANEL=false

# Local-only requester-scoped Lost output, throttled to once per 30 seconds.
LOST_CONSOLE_DEBUG_ENABLED=false
